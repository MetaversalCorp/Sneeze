[package]
name = "solar_system"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
sneeze = { path = "../../../sdk/rust" }

[profile.release]
opt-level = "s"
lto = true
